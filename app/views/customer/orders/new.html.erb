<!--注文情報入力フォーム-->
<%= form_with model: @order, url: orders_confirm_path(id: @order.id), local: true do |f| %>
  <h4>支払方法</h4>
  <%= f.radio_button :order, :payment %>
  <%= f.label :payment, "クレジットカード", {value: :payment} %></br>
  <%= f.radio_button :order, :payment %>
  <%= f.label :payment, "銀行振込", {value: :payment} %>
<%# end %>

<%#= form_with model: @order, url: orders_confirm_path(id: @order.id), local: true do |f| %>
  <h4>お届け先</h4>
  <%= f.radio_button :order, :address %>
  <%= f.label :address, "ご自身の住所", {value: :order} %></br>
    <%= @customer.address %></br>
  <%= f.radio_button :order, :address %>
  <%= f.label :address, "登録済み住所から選択", {value: :order} %></br>
    <%= f.collection_select :address, Receiver.where(customer_id: current_customer.id), :id, :address, include_blank: '選択してください' %></br>
  <%= f.radio_button :order, :address %>
  <%= f.label :address, "新しいお届け先" %></br>
    <%= f.text_field :postal_code %></br>
    <%= f.text_field :address %></br>
    <%= f.text_field :name %></br>
  <%= f.submit "確認画面へ進む" %>
<% end %>