<h3>ジャンル検索</h3>

<%#= form_with model: @product, url: products_path, method: :get, local: true do |f| %>
<%#= f.collection_select :genre_id, Genre.all, :id, :type, include_blank: '選択してください' %>
<%#= f.submit '検索' %>

<% @genres.each do |genre| %>
  <%= link_to genre.type, search_path(genre.id) %>
<% end %>